import React from 'react';
import styles from './ReportsChart.module.scss';

export default function ReportsChart({ filteredData, totalAll }) {
  console.log('hellloo')

  return (
    <div className={styles.reportsChart}>Here's the chart</div>
  )
   
  }
  